services:
  app:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: notesuser
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: notesdb
    volumes:
      - notes-data:/var/lib/postgresql

volumes:
  notes-data:
